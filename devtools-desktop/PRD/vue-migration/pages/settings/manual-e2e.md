# 系统设置真实 Tauri 手动 E2E 清单

> 状态：PG4 与 PG5 清理后回归全部通过
> 日期：2026-07-29
> 目的：验证浏览器无法覆盖的 Tauri IPC、系统通知、Sidecar 重启和真实更新链路。
> 完成条件：逐项确认结果；出现问题时记录操作步骤和实际现象，暂停 PG5。

## 开始前

1. 确认当前没有正在构建、部署、上传或运行中的重要任务。
2. 先记下当前主题、连接超时和侧边栏顺序，测试完成后恢复原值。
3. 不要在文档、截图或聊天中粘贴 GitLab Token、密码、服务器地址等敏感内容。
4. 本轮只允许删除测试时新建的备份，不要删除原有备份；不要真正恢复数据库，只验证恢复确认弹窗并取消。

## A. 页面与布局

- [x] 打开“系统设置”，确认只显示一套新版页面，不会看到旧版七段式长页面。
- [x] 默认窗口下顶部、四个状态摘要、分类导航和当前分类完整显示，没有横向滚动条、重叠或卡片内容越界。
- [x] 依次打开“常规、数据与备份、外观与通知、Git 活动、高级、关于”，六个分类均能正常切换。
- [x] 在顶部搜索“备份”，确认出现“数据备份”结果；点击结果后跳到“数据与备份”并自动清空搜索。
- [x] 缩窄软件窗口，确认分类导航变成顶部横向布局，页面没有横向滚动条；恢复默认窗口大小。

## B. 主题、菜单和普通设置

- [x] 在“外观与通知”依次选择“系统、亮色、暗色”，三种模式均生效；最后恢复原主题。
- [x] 修改连接超时为一个 5～300 的测试值，离开输入框后出现保存成功提示；切换页面再回来仍为新值；最后恢复原值。
- [x] 在侧边栏顺序中把一个菜单下移，确认左侧菜单同步变化；再上移恢复，重新进入系统设置后顺序仍然保持。页面不再提供拖拽入口。
- [x] 展开“实验功能”，切换 Live2D 或点击粒子后全局效果同步变化；测试完成后恢复原开关。

## C. 数据与 Git 活动

- [x] “数据与备份”默认只显示最近 3 份；点击“查看全部”后显示完整列表。
- [x] 点击“立即备份”，确认出现成功提示且新备份进入列表。
- [x] 对刚创建的测试备份点击“恢复”，确认说明中明确“重启后生效”和保留 pre-restore 副本，然后取消，不实际恢复。
- [x] 如果需要清理本轮测试备份，只删除刚创建的这一份，并确认列表更新；不要删除原有备份。
- [x] 打开“Git 活动”，确认现有作者和仓库列表完整；Token 以密码形式显示，不复制或截图。
- [x] 临时修改作者文字，确认出现“有未保存修改”；恢复原文字后应回到“配置已保存”。如确需验证保存，只保存不含敏感改动的原配置。

## D. Tauri 专属能力

- [x] 正式软件的“高级”分类不显示“测试 Sidecar”管理区。
- [x] 在没有运行任务时点击“重启”并确认，页面显示重启状态，随后 Sidecar 恢复在线、端口更新，其他未迁移页面仍能正常读取数据。
- [x] 若系统通知已授权，点击“发送测试”，确认 macOS 收到 DevTools 测试通知；若未授权，确认页面能正确提示权限状态，不强制授权。
- [x] 点击“检查并更新”，先出现影响说明确认弹窗；取消后不会开始更新。
- [x] 在确认工作区允许打包更新后再次开始更新，确认只出现一个新版任务弹窗，进度、日志和失败/完成状态会更新，完成后软件自动退出并重新打开。
- [x] 软件重新打开后再次进入系统设置，确认主题、连接超时、菜单顺序、Git 配置与备份仍然存在。

## E. 回归

- [x] 随机打开首页、纯净检测、工时内容和个人笔记，确认页面仍能正常显示和操作。
- [x] 左侧菜单切换、主题切换和 Sidecar 重启后没有出现双页面、空白页或持续加载。

## 结果

- 2026-07-29：用户确认除拖拽排序外，其他真实 Tauri E2E 均已通过。拖拽未改变顺序且不易使用，已按反馈删除；只需复测 B 区的上下箭头排序，不需要重复其他通过项。
- 2026-07-29：用户完成箭头排序复测并确认没有问题；设置页 PG4 真实 Tauri E2E 全部通过。
- [x] 全部通过，可以进入 PG5 清理旧 `settings.js`、旧设置 DOM 和旧 CSS。
- [ ] 存在问题，已记录复现步骤；暂不进入 PG5。

## PG5 清理后简短回归

> 目的：确认删除旧设置实现并拆分共享 CSS 后，新版设置与仍未迁移页面没有明显回归。PG4 已通过的备份恢复、Sidecar 重启、系统通知和真实应用更新等高风险流程无需重复。

- [x] 打开“系统设置”，确认只显示唯一一套新版页面，六个分类均可切换。
- [x] 分别切换亮色与暗色，确认设置顶部、分类、卡片、输入框和按钮没有明显样式异常；最后恢复常用主题。
- [x] 在“常规”中把一个非固定菜单项上移或下移，确认左侧菜单立即同步；再恢复原顺序。
- [x] 在“外观与通知 → 实验功能”打开一次点击粒子，点击页面确认效果出现，然后关闭。
- [x] 分别打开首页、本地运行和部署面板，确认共享 CSS 拆分后页面没有明显错位、控件丢失或样式破坏。
- [x] 关闭并重新打开“系统设置”，确认页面仍正常且没有旧设置页面闪现。

### PG5 结果

- [x] 2026-07-29 用户确认全部通过，可以关闭 Phase 5-2 并创建本轮本地提交。
- [ ] 存在问题，已记录复现步骤；暂不进入 Phase 5-3。
